<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.warehouse.mapper.ImpSubMapper">

<insert id="checkAdd">

	INSERT INTO impsub (impnum, impitemname, itemname, itemquantity, itemprice) VALUES (#{impnum}, #{impitemname}, #{itemname}, #{itemquantity}, #{itemprice}) 
	
</insert>

<select id="getList" resultType="com.warehouse.domain.ImpSubVO">
	
	SELECT
	
	impnum,
	impitemname,
	itemname,
	itemquantity,
	itemprice
	 
	FROM impsub
	 
	WHERE impnum = #{impnum}
	

</select>

<delete id="checkDel">

	DELETE FROM impsub WHERE impitemname = #{impitemname}

</delete>

<update id="checkModify">

	UPDATE impsub SET
		impnum = #{impnum},
		impitemname = #{impitemname},
		itemname = #{itemname},
		itemquantity = #{itemquantity},
		itemprice = #{itemprice}
	WHERE impitemname = #{impitemname} 

</update>



</mapper>